export interface Constants {
  telegram: {
    token: string;
  };
  africastalking: {
    apiKey: string;
    username: string;
    chat: {
      productId: string;
      whatsapp: {
        channelNumber: string;
      };
      telegram: {
        channelNumber: string;
      };
    };
  };
}
